module QBT {
    /** Commented out -- turns out that PowerBank is an artifact of older releases of ISA
                         so you don't actually need to define this
                         modern ISA uses a Moveable object with a particular sprite
    item BaseTeleporter
    {
        Type         = Moveable,
        DisplayName  = Moveable,
        Icon         = default,
        DisplayCategory = Electronics,
        WorldObjectSprite = baseteleporters_tileset_01_0
    }
    **/
}